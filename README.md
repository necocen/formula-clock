# Formula Clock

## Run locally

Requires **pnpm 12.4.1** and **Python 3.11+**. pnpm installs the required Node.js version and dependencies.

From the repository root:

```sh
pnpm install
pnpm run dev
```

Open [http://127.0.0.1:8787](http://127.0.0.1:8787).
