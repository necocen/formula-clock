# Formula Clock — Codex引き継ぎ

基準版：**r5-stix2 / package 5.0.0**  
引き継ぎ資料作成日：2026-09-08

## 0. 現在地

HHMMの4桁で現在の秒SSを計算する数式時計。ブラウザで表示する。
ユーザーは現状の見た目と動きをほぼ完成と評価している。最新の選択は **STIX Twoのオールドスタイル数字**。
このZIPはその版のソースとデータを保持し、引き継ぎ資料と検証ログを追加したもの。時計本体に新機能や仕様変更は加えていない。

**最初にすることは、現状の起動とテスト、配布設定のMathJax 4＋STIX Twoの確認。**
その後の実装はユーザーの次の依頼に従う。フレームワーク移行、全面的なUI変更、ソルバの改造を要求されているわけではない。

読む順番：このファイル → `AGENTS.md` → `FORMAT.md` → 変更対象のソース。
検証状況は `docs/VERIFICATION.md`、操作による確認項目は `docs/ACCEPTANCE.md`。

## 1. 起動・ビルド・テスト

プロジェクトルートは、このファイルがある `formula-clock/`。

```sh
# Node.js 20以上。ビルドとNodeテストに外部npmパッケージは不要。
npm test
npm run build

# 開発用HTTPサーバー。別ターミナルで継続して起動する。
python3 -m http.server 8000 --bind 127.0.0.1
# ブラウザで http://127.0.0.1:8000/ を開く。
```

`index.html` は生成済みで、ファイルをブラウザで直接開くこともできる。
アプリと式データは同じHTMLに入っているが、**MathJax本体と書体のデータはCDN依存**。
単一HTMLであることと、オフラインで起動できることは別の条件になる。

```sh
# 1分ごとのJSONを非同期で取得する配信版を生成する場合
npm run build:external
python3 -m http.server 8001 --bind 127.0.0.1 --directory dist-external
# http://127.0.0.1:8001/
```

`dist-external/` は再生成できるため、このZIPには含めていない。
全日データを作り直すコマンドは `npm run generate`。通常の起動やビルドでは実行不要。

### ブラウザテストの準備

```sh
python3 -m venv .venv
. .venv/bin/activate
python -m pip install -r requirements-test.txt
python -m playwright install chromium
python tests/browser.test.py --screenshots
```

`requirements-test.txt` は既存テストのimportを整理した依存一覧で、バージョン固定済みのロックファイルではない。
ブラウザテストの既定経路は、配布版のMathJax 4.1.3をCDNから取得するもの。
ローカルMathJax 3を使う互換テストとは区別する。詳細は `docs/VERIFICATION.md`。

## 2. ユーザーが重視している仕様

### 数字が同じ物体として動く

- 4桁を左から一度ずつ使う。数字の同一性は値ではなく元の桁位置で決める。
- たとえば12:34:30の `(12 + 3) × √4 = 30` から、12:34:31の `−(1 + 2) + 34 = 31` へ移る。
- このとき `1, 2, 3, 4` は常設の同じSVG要素。位置と倍率を変える。`12` や `34` に連結されても、各桁は別の要素。
- 各桁の色を維持する。同じ数字が複数あっても、桁位置に対応した色と要素を区別する。
- 記号・括弧・分数線・根号などは、数字とは別の層でフェードする。
- 指数や分母へ移る数字は、組版結果どおりに移動・縮小する。
- 秒の2桁にも常設SVG要素がある。時分が変わり数字自体が変わる場合は、要素を保持して字形を更新する。

### 式がない秒

提供データが `null` の秒は **彩度と濃さを落とした `HH:MM:SS`** を表示する。
この淡い表示はユーザーが明示的に気に入っている。以前の「ｽﾝｯ」という文字を出す表示には戻さない。
通常時刻の表示でもHHMMの4要素を継続して使う。

組版エンジンの読み込み前や故障時は、別途通常テキストの時刻表示へ退避する経路がある。
これは正常に組版できる `null` 秒のSVG表示とは別の経路。

### 縦方向の基準

**等号の実際の字形の上下中央を、表示領域の一定の高さに固定する。**
式全体の外接矩形を毎秒縦中央に置くと、根号や分数が出ただけで通常の数字まで上下に揺れるため、その方式は採用しない。
横方向は式全体を中央に配置してよい。

同じ倍率なら通常位置の数字の基線も安定する。幅や上下の余裕に合わせて式全体を縮小する場合は倍率が変わるため、数字の全座標が不変になる保証ではない。
縮小時も等号の中心は固定する。分数・累乗そのものに必要な上下移動は残す。

### フォントの好みと現在の選択肢

| ID | 書体 | 数字と記号の高さ |
|---|---|---|
| `stix2` | STIX Two · Oldstyle（初期値） | オールドスタイル数字。記号は本来の数式軸。持ち上げ補正なし |
| `euler` | Euler | 通常数字。数字の中央に記号を合わせる補正あり |
| `oldstyle` | Computer Modern · Oldstyle | オールドスタイル数字。本来の数式軸 |

ユーザーはComputer Modernの数字を古風すぎると感じ、STIX Twoのすっきりした形、特に6と9の空間を好んだ。
Fira Sansの見本も比較したが、今回採用する方向ではない。STIX Twoを初期値にしてある。
Computer Modernの選択肢は比較用に残っている。明示的な依頼なしに選択肢を増やす必要はない。

書体と除算表記を `localStorage` の `formula-clock-display-v2` に保存する。
保存済み設定があればそちらが優先されるので、初期値確認時は新しいブラウザコンテキストを使う。

### 除算表示

「分数」と「÷」を切り替える。同じ構文木に対して表示時に括弧を付け直す。
たとえば `a / (b / c)` は `a ÷ (b ÷ c)`、`(a / b) / c` は `a ÷ b ÷ c`。
データ側に表示用括弧やTeXを埋め込まない。

### 時計と音

端末のローカル時刻を使う。毎分00秒に「ポーン」。23:59:57、58、59に予告音を鳴らし、00:00:00の時報へつなぐ。
現実装は予告440 Hz、時報880 Hzの正弦波。初期状態は音オフで、ユーザー操作で有効化する。
プレビュー、再生・停止、0.5倍速、全画面表示、秒の目盛りからの選択、時刻指定を備える。
音の予定は表示処理から独立し、プレビュー変更時などに古い予定を取り消す。見逃した時報を後からまとめて鳴らさない。

## 3. ファイルと処理の分担

```text
外部の生成器 / tools/solver.cjs
  → data/expressions.json（式の構文木）
  → data.js（提供元に依存しない1分単位の取得）
  → expression.js（桁の識別子、優先度・結合性、TeXの生成）
  → typesetter.js（MathJaxでSVGを生成、字形と座標を抽出）
  → app.js（時計、常設の数字要素、アニメーション、操作、音）
```

| ファイル | 役割 |
|---|---|
| `_head.html` | ページの骨格、CSS、操作UI。通常の見た目変更はここ |
| `expression.js` | AST検証と、表示設定を考慮したTeX生成 |
| `data.js` | `InlineProvider` / `TableProvider` / `FetchMinuteProvider`、入力の正規化、埋め込みデータの展開 |
| `typesetter.js` | 書体プロファイル、MathJax起動、数式軸、SVG測定、組版キャッシュ |
| `app.js` | 時計、データ先読み、描画、アニメーション、フォールバック、UI、時報 |
| `build.cjs` | ソースを連結してHTML生成。埋め込み／外部データの両方式 |
| `index.html` | 現在の配布用生成物。編集の原本にはしない |
| `data/expressions.json` | 1,440分、86,400秒の事前生成データ |
| `data/example-1234.json` / `data/example-table.json` | 受け渡し用の完全なサンプルJSON |
| `FORMAT.md` / `api.d.ts` / `formula.schema.json` | データと公開APIの仕様・型・スキーマ |
| `tools/solver.cjs` / `tools/generate.cjs` | オフラインの式探索・全日データ生成 |
| `tests/` | Nodeテスト、ブラウザテスト、以前の互換テスト結果と画面見本 |

依存するUIフレームワークはない。通常のJavaScriptとHTML/CSSで構成する。
生成物を直接直すと再ビルドで失われるので、ソースを変更して `npm run build` を実行する。

## 4. 組版・アニメーションで維持する点

### 組版エンジンと描画を分ける

MathJaxのSVG全体をそのまま画面上で置換しない。
`\cssId{fc-d0}{...}` などで数字の位置を識別し、各字形のパスと変換行列を取り出す。
表示する側はHHMMの4要素と秒の2要素を保持する。記号類は別のSVGグループとして入れ替える。
`fontCache: 'none'` の設定で、数字を直接のパスとして取り出す実装になっている。

### 座標系の過去の不具合

画面外の測定用SVGから数字を取り出した際、記号だけ見えて数字が消える不具合があった。
現在はルートと字形の両方に **`getScreenCTM()`** を使い、共通の座標系を経由して相対変換を求める。
実際の字形パスを基準にする。`cssId` のラッパーには補正用の階層が入る場合がある。
この処理と座標検証を安易に `getCTM()` やラッパーの単純な位置測定に戻さない。

### 書体ごとに独立したエンジン

MathJaxは非表示iframe内に書体ごとに作成し、必要になったときに読み込んで保持する。
Euler拡張と数式軸の変更がSTIX TwoやComputer Modernへ混ざらないようにするため。
設定の配列・オブジェクトはiframe側のJSONパーサーで作る処理がある。そのrealmの違いへの対応を維持する。

配布設定はMathJax **4.1.3**、`tex-svg-nofont.js`。書体プロファイルは `typesetter.js` の `PROFILES`。
STIX Twoは `mathjax-stix2` と各数字の `\oldstyle` を使用する。

### 高さの調整

Eulerだけ、実際の `0` の上下中央を起動時に一度測り、`font.params.axis_height` と記号の `\vcenter` を調整する。
STIX TwoとComputer Modernは `numericAxis: false` / `centerOperators: false`。
書体を変えても最終的な等号の字形中心を `stage.height / 2` に置く処理は共通。

MathJaxの `font.params` やSVG構造へのアクセスはバージョン依存の接点。ライブラリ更新時は実際の出力で確認する。

### 非同期処理

組版結果は直列化してキャッシュし、直後の秒を先に組版する。
`requestSerial` によって、時刻・書体・表示設定を変えた後に古い結果が届いても表示しない。
データ提供元の差し替えは `dataRevision` と `AbortController` を使う。
中止に応じない提供元からの応答も採用しない。

## 5. データの正規形式

詳細は `FORMAT.md` が基準。r5でもスキーマは **`formula-clock/1`** のまま。

```ts
type Expr =
  | { op: 'lit'; i: number; j: number }
  | { op: 'neg' | 'sqrt' | 'fact'; a: Expr }
  | { op: 'add' | 'sub' | 'mul' | 'div' | 'pow'; a: Expr; b: Expr };

type MinuteRecord = {
  schema: 'formula-clock/1';
  hhmm: string;
  seconds: (Expr | null)[]; // 正確に60要素
};
```

`lit` はHHMMのゼロ始まりの区間 `[i,j)`。
`{op:'lit',i:0,j:2}` は `"1234"` の `12` を表すが、描画時には2桁を別要素にする。
現行規則は4桁を順番に各1回使い、複数桁の先頭0と挿入定数を認めない。
等号と右辺SSはフロントエンドで付け、ASTには左辺だけを保存する。

```js
FormulaClock.setDataProvider(
  new FormulaData.FetchMinuteProvider(hhmm => `/expressions/${hhmm}.json`)
);

// 提供元の共通契約：getMinute(hhmm, {signal}) → Promise<MinuteRecord>
```

保存形式をTeXに変えない。構文木なら桁の同一性と除算の意味を維持しつつ、書体・括弧・分数/÷を選べる。
構文木からTeXを生成する際、`a!!` と `(a!)!`、負の底、除算・減算の右辺を区別する。
演算の意味を変える代数的な簡約は表示側で行わない。

今回のデータには71,456秒分の式、14,944個の `null` がある。
`null` は提供する式がないという意味であり、数学的に不可能という証明ではない。
生成ソルバは探索コスト・候補数・中間値に制限がある。
12:34の30/31/59秒には、ユーザーが最初に示した3例を選ぶ処理が生成器にある。

データ受け取り時には構造・桁の使用順などを検証するが、**秒との計算結果の一致は生成側で検証する**。
表示用JavaScriptへソルバや数値評価器を戻さない。

埋め込みか非同期取得かは、ユーザーが後で決めるとした事項。
今回の配布HTMLは試用向けのgzip＋base64埋め込みで、配信方式を固定した決定ではない。

## 6. 検証済み範囲と未検証範囲

今回の引き継ぎ準備で実行したもの：

- `npm test`：全日データの厳密評価・桁・TeX生成とプロバイダーのテストが成功。
- `npm run build`：生成HTMLが前回渡した最新版HTMLとSHA-256まで一致。
- `npm run build:external`：生成に成功。1,440件のJSONを元データと照合。

**今回ブラウザテストは再実行していない。**
`tests/stix2-compatibility-results.json` と `tests/screenshots/` は前回のローカル互換テストの記録。
そのエンジンはMathJax 3.2.1で、STIX Two Text/Mathのローカル字形・寸法をテスト用に設定したもの。
60配置ケースの記録があり、見た目の参考にはなるが、配布版MathJax 4＋CDNの検証結果ではない。

以前の実行環境ではCDNへ接続できず、MathJax 4.1.3＋STIX Two / Eulerの配布経路は未確認と説明している。
CDNに接続できる環境で、まずそこを確認する。特にSTIX Twoの6/9が見本の字形か、別書体に置き換わっていないかを実画面で見る。

以前ユーザーから以下のコンソール警告が報告されている。

```text
Component [mathjax-euler-extension]/svg uses 4.0.0 of MathJax; version in use is 4.1.3
```

数字が消える問題は座標変換修正後にユーザーから改善が確認されたが、この警告の扱いは配布環境で別途確認する。
ログを消すだけで検証完了とはしない。

## 7. 次に検討する事項（追加実装の確定依頼ではない）

1. 配布版のMathJax 4＋3書体を実際に起動し、Chromiumに加えてFirefoxとSafariで確認する。
2. 取得するMathJax本体・フォント関連データのバージョンと読み込み経路を記録する。
3. 埋め込み／全日一括取得／分ごとの取得の採用方式を、公開方法に合わせてユーザーと決める。
4. 必要ならブラウザテスト環境と依存バージョンを固定し、継続的に再現できるようにする。

小さな表示上の残件として `_head.html` の初期 `#engine-status` 文言にEuler表記が残る。
JavaScript開始後は選択中の書体名になる。修正する場合は `_head.html` で変更して再ビルドする。

新しい演算、複数候補の選択、書体追加、データ圧縮方式の変更はまだ依頼されていない。
今の設計を維持したまま、後続の要望に必要な変更から進める。

## 8. 見本と過去の変更

- `docs/images/stix2-preview.png`：最後に提示したSTIX Twoの式の見本。
- `docs/images/numeral-comparison.png`：STIX TwoとFira Sansを比較した数字見本。
- `tests/screenshots/`：前回のSTIX Two互換テストの全画面・狭い画面の画像。

これらのSTIX Twoの見本は前述のローカル字形を使った描画。CDN版の実行スクリーンショットではない。

変更の流れ：

| 段階 | 変更 |
|---|---|
| 初期 | 自前の組版とブラウザ内ソルバ、数字の移動・色分け |
| Euler版 | MathJaxのSVG組版へ移行。式がない秒は普通の時刻表示へ |
| r2 | 数字の座標変換を共通座標系に統一 |
| r3 | Eulerの記号の高さを補正し、等号の縦位置を固定 |
| r4 | 構文木データの事前生成、プロバイダー分離、分数/÷、書体切り替え |
| r5（現在） | STIX Two Oldstyleを追加・初期値に。式スキーマは変更なし |

古い実装を混在させず、現在の `index.html` と対応するソースだけをこのプロジェクトの基準にする。

## 9. Codexへの最初の指示例

> CODEX_HANDOFF.mdとAGENTS.mdを読み、まずnpm testとビルドを確認する。
> 現在のUI・アニメーション・STIX Two初期設定を維持する。
> 配布版MathJax 4.1.3＋STIX Twoの動作をブラウザで確認し、
> 実際に確認できた経路とローカル互換テストだけの経路を区別して報告する。
> 追加の仕様変更は、次に渡す依頼に基づいて進める。
